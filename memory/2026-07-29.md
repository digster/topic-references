# 2026-07-29 — Redo the first two topic pages (Transformers, Starting a New Bank)

## What was done
Full rewrites of the repo's two oldest topic pages, both originally written by a non-Opus-5 model.
The DSA page (`2026-07-28`) was the quality bar: learning-path ordering, notes that say *why* a
resource earns its place, paid resources included with the cost disclosed.

- **`topics/transformers.html`** — rewritten, **32 resources**, `Last updated: 2026-07-29`.
- **`topics/starting-a-new-bank.html`** — rewritten, **32 resources**, `Last updated: 2026-07-29`.
- **`index.html`** — both cards got new one-line descriptions and expanded `data-tags`; alphabetical
  order and the `<script>` filter untouched.

## Scope decisions (asked, answered by the user)
1. **Transformers → foundations + modern stack.** Fundamentals stay the spine; the page now also
   covers what a 2026 model actually does — RoPE, FlashAttention, MoE, scaling laws, mechanistic
   interpretability, and Mamba as the live alternative to attention.
2. **Starting a New Bank → options 1 *and* 4 blended.** The description and economics resources are
   jurisdiction-agnostic (deposits, net interest margin, rails); the US supplies the detailed worked
   path; UK (PRA/FCA) and India (RBI) primary sources sit alongside the US ones. Regulation stays
   light, per the original 2026-06-23 instruction on this topic.
3. **Depth → match the DSA page.** Landed at 32 each (DSA has 27); trimmed one redundant BaaS vendor
   row rather than padding out to a target.

## Link rot found and fixed (inherited from the old pages)
- `coursera.org/specializations/wharton-fintech` → **hard 404** (both `curl` and `WebFetch`; search
  engines still index it). Replaced with the University of Michigan *Financial Technology (Fintech)
  Innovations* specialization, flagged paid — its FAQ states it cannot be audited free.
- `http://nlp.seas.harvard.edu/annotated-transformer/` → switched to `https://`.

## Verification notes (this is the part worth re-reading)
- **A 200 status is not proof the page exists.** `rbi.org.in/commonperson/English/Scripts/
  BankLicensing.aspx` returns **HTTP 200 serving an error page** ("The file you are looking for is
  not available"). Only a content fetch caught it. Replaced with the real press release for the
  *Guidelines for 'on tap' Licensing of Universal Banks in the Private Sector* (prid=37658, 1 Aug
  2016), content-verified.
- **`github.com` returns 403 to `curl` from this container** — bot blocking, not dead links. All four
  GitHub URLs (`nanoGPT`, `nanochat`, `TransformerLens`, `rasbt/LLMs-from-scratch`) were verified by
  `WebFetch` instead.
- **`youtube.com` returns 429 to `curl`** — rate limiting. All video IDs and the two playlists were
  verified through the `oembed` endpoint, which returns title + channel and is not rate-limited.
- **`huggingface.co/learn/llm-course/` 503s on `WebFetch`** but serves 200 to `curl`; kept on the
  strength of the `curl` check plus it being the canonical URL.
- **Deliberately excluded:** Bishop's *Deep Learning: Foundations and Concepts* — `bishopbook.com`
  answers 202 behind a challenge and the content could not be confirmed. *Digital Bank* (Skinner,
  2014) — dropped as stale, superseded by *Bank 4.0* and *Banking On It*. Treasury Prime — cut as
  redundant once Column, Unit and Stripe Treasury were all on the page.
- **nanoGPT is deprecated** in favour of Karpathy's `nanochat` (Nov 2025). Kept nanoGPT as the
  teaching repo it still is, with the successor linked in its note.

## Verification
- `uv run tests/check_site.py` → `✓ OK — 3 topic page(s), all linked and well-formed.`
- Full `curl` sweep of every link on both pages: all 200 except the known GitHub 403 / YouTube 429
  blocks described above, each verified another way.
- **34/34 Playwright checks passed** against `python3 -m http.server` (`file://` is blocked here).
  Covers: 3 cards in alphabetical order, count reads "3 topics", the *new* tags filter correctly
  (`flashattention` → Transformers, `rbi` → Starting a New Bank), nonsense query shows the
  no-results message, clearing restores, card href navigates, description precedes resources, all
  seven sections in contract order on both pages, every external link `target="_blank"
  rel="noopener"`, `data-level` limited to the three styled values and actually styled, no leftover
  `{{PLACEHOLDER}}`, no horizontal overflow at 1280px *or* 390px, back link returns home.
- **Gotcha for future sessions:** the Brutalist Mono theme applies `text-transform: uppercase`, so
  Playwright's `inner_text()` returns UPPERCASE. Compare case-insensitively or you'll chase eight
  phantom failures.
- No screenshots were written, so nothing to clean up.

## Docs updated
- `PROMPT.md` — appended the 2026-07-29 entry.
- `README.md` — **not** changed; it carries no enumerated topic list, and its line about Transformers
  shipping as the sample topic is still true.
- `ARCHITECTURE.md` — **not** changed; no structural change, and the file tree uses a generic
  `<slug>.html`.

## Still open
- `CLAUDE.md`'s "Verifying your work" section still says to check that dark mode works, but the
  Brutalist Mono re-theme (`58f4b85`) removed `prefers-color-scheme: dark`. That line has now been
  stale for two sessions. Flagged to the user; not changed unilaterally.

---

# Later same day — Add topic: Machine Learning

## What was done
- **`topics/machine-learning.html`** — new page, **33 resources**, `Last updated: 2026-07-29`.
  All seven sections filled (Books 6, Papers 6, Articles 4, Videos 3, Courses 6, Tools 3,
  Further Reading 5).
- **`index.html`** — one card added at alphabetical position 2 (between DSA and Starting a New
  Bank). `<script>` filter untouched, as always.

## Scope decisions (asked, answered by the user)
The prompt was explicit that AI gets its own page later, and `topics/transformers.html` already
covers deep learning — so the page had to be carved to sit between two neighbours.
1. **Deep learning → out of scope.** Page is classical/statistical ML **plus production/MLOps**.
   DL appears exactly once, as a cross-link to the Transformers page in Further Reading.
2. **Theory depth → balanced ladder.** Beginner (Burkov, Google Crash Course, MLU-Explain,
   Kaggle Learn) → applied core (ISLP, Géron, scikit-learn) → rigorous tail (ESL, *Understanding
   Machine Learning*, Murphy, MML).
3. **Title → plain "Machine Learning"**, slug `machine-learning`. The card's one-liner does the
   disambiguating rather than a parenthetical.

## Link rot and traps found
- **R2D3, *A Visual Introduction to Machine Learning*** — `www.r2d3.us` returned **HTTP 522**
  (Cloudflare, origin down) on every check across ~45 minutes. **Dropped**, not archived-around.
  Worth re-testing next time someone touches this page; it's a genuinely great beginner link.
- **Breiman, *Statistical Modeling: The Two Cultures*** — Project Euclid shows an Open Access icon
  but **paywalls the full text at $30**; only a content fetch revealed that. Now links a
  content-verified free course copy (`stat.uchicago.edu/~lekheng/courses/191f09/breiman.pdf`,
  confirmed as the real 33-page article), with the paywall noted.
- **Papers with Code is sunset** — `paperswithcode.com` 302s to `huggingface.co/papers/trending`.
  Excluded.
- **Elements of Statistical Learning PDF paths are all dead** — every
  `hastie.su.domains/ElemStatLearn/printings/ESLII_print*.pdf` 404s; the live free PDF is served
  through a Google Drive redirect from `ESLII_print12_toc.pdf.download.html`. Linked the **book
  home page** instead and mentioned the download page in the note — stabler than either.
- **Google Cloud MLOps doc moved** — `cloud.google.com/architecture/...` 301s to
  `docs.cloud.google.com/architecture/...`. Used the new host directly.
- **Coursera ML Specialization has no free audit** (page states paid subscription, financial aid
  available). Note says so, and points at CS229 as the free equivalent.

## Verification notes (new gotchas worth keeping)
- **`cs.huji.ac.il` sits behind a JS bot-challenge**: it returns **HTTP 200 with an obfuscated
  challenge script or a "Something went wrong" page** to `curl`, and blank to `WebFetch`. The book
  is real and live — proven by `WebFetch` pulling the full **449-page PDF**, whose first page reads
  *"Please link to: http://www.cs.huji.ac.il/~shais/UnderstandingMachineLearning"*. Linked the
  landing page the authors ask for. Another instance of "200 means nothing" from the last session.
- **Playwright cannot reach external hosts here.** The container routes outbound HTTPS through the
  agent proxy, which Chromium doesn't use, so every external `page.goto` fails with
  `ERR_CONNECTION_RESET`. External URLs must be verified with `curl`/`WebFetch`; Playwright is for
  the local site only (run it with the proxy env vars unset and `NO_PROXY='*'`).
- **The bundled Chromium is build 1194, but a fresh `uv --with playwright` pulls a client expecting
  1228.** Do **not** run `playwright install`. Launch with
  `executable_path="/opt/pw-browsers/chromium-1194/chrome-linux/chrome"` and `--no-sandbox`.
- **`pypdf` under plain `uv run` crashes** (`_cffi_backend` missing — the system `dist-packages`
  leaks in). Use `env -u PYTHONPATH PYTHONNOUSERSITE=1 uv run --isolated --with pypdf`. This is the
  fastest way to prove a PDF is the paper it claims to be; poppler/`pdftotext` is **not** installed,
  so the `Read` tool can't render PDFs either.
- Confirmed still true from last session: `github.com` 403s to `curl`, `oreilly.com` 403/000s,
  YouTube 429s (use `oembed` — but it starts answering `Unauthorized` after ~6 rapid calls, so space
  them out; playlist titles can also be read from the page `<title>` with a browser UA).

## Verification
- `uv run tests/check_site.py` → `✓ OK — 4 topic page(s), all linked and well-formed.`
- Full `curl` sweep of all 37 external links; everything 200 except the known bot-blocked hosts,
  each confirmed another way. Four PDFs (Breiman ×2, Domingos, *Understanding ML*) were opened and
  their first pages read to confirm identity.
- **34/34 Playwright checks passed** against `python3 -m http.server`. Covers: 4 cards alphabetical,
  ML card second, count reads "4 topics", `data-tags`-only terms filter correctly (`xgboost`,
  `mlops`, `kaggle` → ML card), nonsense query shows the no-results message and clearing restores,
  card href navigates, description precedes resources, prereq box renders, seven sections in
  contract order, 33 items each with a note, `data-level` limited to the three styled values with
  all three used, every external link `target="_blank" rel="noopener"`, no external CSS/JS/img, the
  internal `transformers.html` cross-link resolves 200, both back links return home, no horizontal
  overflow at 390px.
- Case-insensitive comparisons throughout — the uppercase-heading gotcha from earlier today.
- No screenshots written, so nothing to clean up.

## Docs updated
- `PROMPT.md` — appended the "Add topic: Machine Learning" entry.
- `ARCHITECTURE.md` — **not** changed. No new section type, file kind or layout; the file tree
  already uses a generic `topics/<slug>.html`.
- `README.md` — **not** changed; it carries no enumerated topic list and usage is unchanged.

## New pattern introduced
First **internal cross-link between two topic pages** (ML → Transformers, in Further Reading). Kept
as a plain relative `href="transformers.html"` with **no** `target="_blank"` — the blank-target rule
in `CLAUDE.md` is about *external* links. `check_site.py` is unaffected: it only parses
`href="topics/X.html"` occurrences in `index.html`. If cross-linking becomes common, it may be worth
a line in `CLAUDE.md`'s house style.

## Still open
- ~~`CLAUDE.md`'s "Verifying your work" section still tells you to check dark mode~~ — **resolved
  later the same day**, see below.
- R2D3 is worth re-checking; if it comes back it belongs in the Articles section as the beginner
  visual intro.

---

# Later same day — Confirm no dark mode

The user confirmed: **the site is not getting dark mode for now.** That settles the stale
verification step flagged in the two previous sessions.

- **`CLAUDE.md`** — the "Verifying your work" bullet no longer says to check that "dark mode works
  (it follows the OS setting)". It now checks that the level pills are colored, and states plainly
  that the theme is deliberately fixed light with no dark mode to check, pointing at
  `ARCHITECTURE.md` for the reasoning.
- **`ARCHITECTURE.md`** — **not** changed. It was already correct: it documents the fixed-light
  choice as deliberate ("there is intentionally **no** `prefers-color-scheme: dark` variant").
- **`PROMPT.md` line 54** — **not** changed. It is a historical log entry recording that the
  re-theme removed the dark block, which is still an accurate account of what happened.
- **`styles.css`** — untouched; there was never anything to remove, the re-theme (`58f4b85`) already
  dropped the `prefers-color-scheme: dark` block.

Net effect: the contract, the architecture doc and the stylesheet now agree, and a future session
won't re-flag this.

---

# Later same day — Add topic: Large Language Models

## What was done
New `topics/large-language-models.html` (**49 resources**, all seven sections) and one card in
`index.html` at alphabetical position 2. The page covers the full LLM arc: next-token prediction and
tokenization, scaling laws (Kaplan → Chinchilla), post-training (InstructGPT/RLHF → DPO), prompting
and chain-of-thought, RAG, LoRA, agents/tool use, evals, reasoning models, and running models
locally. Its description explicitly defers image/audio/video generation to a future topic, mirroring
how the ML page deferred deep learning.

## Scope decisions (asked, answered by the user)
Three questions asked. **The `AskUserQuestion` picker returned empty twice** — once as "did not
answer", once as dismissed — and the user then said they *had* answered. Re-sent the same three
questions as **plain text in the chat**, which worked immediately (`1b / 2c / 3a`).
**Lesson: if the question picker round-trips empty, fall back to plain text rather than re-issuing
the tool.**

- Title → plain **"Large Language Models"**, slug `large-language-models`. No `(Generative AI)`
  parenthetical; `data-tags` carries `generative ai genai` instead.
- Overlap with `topics/transformers.html` → **self-contained, overlap freely** (this is a change of
  posture from the ML page, which deliberately avoided overlap).
- Emphasis → **balanced ladder**: understand them → build one → build *with* one.

## Deliberate duplication with the Transformers page
Per answer 2c, ~11 rows appear on both pages: Attention Is All You Need, GPT-3, Chinchilla, the
Illustrated Transformer, Karpathy's *Let's build GPT* and *Deep Dive into LLMs*, 3Blue1Brown Ch. 5,
the HF LLM Course, Zero to Hero, nanochat, bbycroft's visualizer, Jurafsky & Martin, `rasbt/LLMs-from-scratch`.
This is intentional, not drift — recorded here so a future session doesn't "fix" it. The pages stay
differently shaped (Transformers = architecture-first, stops at the model; LLMs = tokenization →
shipping an evaluated product) and cross-link each other.

## Link rot and traps found
- **Stephen Wolfram, *What Is ChatGPT Doing … and Why Does It Work?* — INCLUDED, but not
  content-verified.** Unreachable from this container on three attempts: HTTP 503 via WebFetch,
  `000` via curl even with a browser UA. Initially dropped, then **added on the user's explicit
  instruction** ("add the most probable link now, if it does not work we can update it later").
  Corroborated indirectly: `WebSearch` returns the URL
  `writings.stephenwolfram.com/2023/02/what-is-chatgpt-doing-and-why-does-it-work/` with a matching
  indexed title, which makes this a container-reachability problem rather than link rot. **This is
  the one row on the page not opened end-to-end — recheck it first if anything looks stale.**
- **`aiengineeringbook.com` is a parked domain for sale** — returns HTTP 200 with the title "A Brand
  New Domain!". Another instance of *200 is not proof*. *AI Engineering* is linked via
  <https://huyenchip.com/books/> instead (content-verified: names the book, O'Reilly 2025).
- **Stochastic Parrots**: the ACM DOI 403s behind a Cloudflare challenge from here (both curl and
  WebFetch). Linked a free PDF instead, content-verified with `pypdf` — 14 pages, correct four
  authors, correct title. A second candidate mirror (`treasures.scss.tcd.ie`) was **rejected: only 4
  pages**, i.e. a partial copy. Checking page count caught what a 200 would not have.
- Simon Willison's annual review: guessed URLs 404'd; the real 2025 post is
  `/2025/Dec/31/the-year-in-llms/` (title "2025: The year in LLMs"), found via search. Don't
  extrapolate his URL slugs from the previous year's.
- Anthropic's agents post is titled **"Building Effective AI Agents"** (not "…effective agents").
- `stanford-cs336.github.io/spring2025/` 301s to **plain-http** `cs336.stanford.edu` — linked the
  https github.io URL deliberately.
- **Own-verification bug worth remembering:** a curl loop that reuses one output file shows the
  *previous* page's `<title>` when a fetch fails, because the file isn't cleared. This nearly
  credited Wolfram's essay with "Prompt Engineering Guide". Always `rm -f` the output file per
  iteration.
- Host behaviour confirmed again, exactly as last session: `github.com` 403s to curl (WebFetch works),
  `oreilly.com` 403s, `dl.acm.org` 403s, `huggingface.co/learn/` fine via curl, arXiv fine via curl
  (`citation_title` meta is the cheapest content check), YouTube `oembed` fine when spaced into
  batches of three.

## Verification
- `uv run tests/check_site.py` → `✓ OK — 5 topic page(s), all linked and well-formed.`
- **30 Playwright checks, all passing** (local site only — external hosts are unreachable):
  5 cards alphabetical with LLM second, count reads "5 topics", `data-tags`-only terms filter
  (`rag`, `rlhf`, `dpo`, `ollama`, `genai`), nonsense query → no-results and clearing restores,
  card href navigates, h1 and today's date, description precedes resources, prereq box renders,
  seven sections in contract order, 49 items each with a note, `data-level` limited to the three
  styled values with all three used and visibly distinct backgrounds, every external link
  `target="_blank" rel="noopener"` (including the one nested in a note), no external CSS/JS/img,
  both internal cross-links 200, both back links return home, no horizontal overflow at 390px.
- Visual check at 1100px on both pages. Screenshots deleted afterwards.
- `pkill -f "http.server 8765"` **killed the calling shell** (exit 144) because `-f` matched the
  command line containing that string. Use `ss -ltnp` to find the pid instead.

## Docs updated
`PROMPT.md` appended. `ARCHITECTURE.md` **not** changed (no structural change — the tree uses a
generic `<slug>.html`). `README.md` **not** changed (carries no enumerated topic list). Third
consecutive session where both were correctly left alone.

## Still open
- R2D3's *A Visual Introduction to Machine Learning* (HTTP 522 last session) — still unrechecked.
- Wolfram's ChatGPT essay is on the page but was never fetched successfully from here — confirm the
  link resolves from a session that can reach `writings.stephenwolfram.com`.
- A future topic covering the **non-LLM parts of generative AI** (diffusion, images, audio, video)
  was promised by the user in this prompt and is referenced in this page's description.

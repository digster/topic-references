# 2026-09-26 — GA vs GP: one topic or two? (advice only, no site changes)

## Prompt

> should generic algorithms and genetic programming be separate topics or they make more sense
> together? Do not make any changes yet.
> https://digster.github.io/topic-references/topics/genetic-algorithms-and-genetic-programming.html

The question was asked in **another session that was opened in the wrong repo**, so it was
answered from the live page without this repo attached. The user pasted that answer into
this session to be recorded here and in `PROMPT.md`. **No topic page, `index.html` or doc was
changed.** Everything below is a backlog for when the user decides.

## Verdict: keep GA and GP on one page

GP is not a separate discipline from GAs. It is the same evolutionary loop with a different
representation: programs (expression trees) evolve instead of fixed-length strings.

- **Same algorithm.** Population, fitness, selection, crossover and mutation work the same way
  in both. Koza introduced GP as Holland's GA applied to LISP trees, and the page's own intro
  frames it that way.
- **Splitting would duplicate most of the page.** Eiben & Smith, Luke, No Free Lunch, NSGA-II,
  specification gaming, DEAP and pymoo apply to both. Two pages would either copy them or send
  readers back and forth.
- **GP alone would be thin.** The answer counted about 9 clearly GP-only resources: Poli's
  *Field Guide*, Koza 1989, Schmidt & Lipson, Cranmer's talk, the gplearn tutorial, PySR, TPOT,
  SRBench and Langdon's bibliography. Checked against the repo, Koza's 1992 book is GP-only too,
  so it is about 10 of 53 external rows. Still a weak page next to the others.
- **It fits the site's pattern.** "Ethereum and Smart Contracts" and "Crypto Economics and
  DAOs" are also "X and Y" topics.

## The real issue: the title and the content don't match

The page covers evolutionary computation as a whole; classic GA is only about a third of it.

| Group | Examples on the page | Approx. share |
|---|---|---|
| Classic GA and shared foundations | Mitchell, Goldberg, Holland, Whitley, PyGAD, Nature of Code | ~⅓ |
| GP / symbolic regression | Poli, Koza, PySR, TPOT, SRBench | ~⅙ |
| Neuroevolution and open-ended search | NEAT, novelty search, MAP-Elites, Karl Sims, MarI/O, Stanley & Lehman | ~⅕ |
| Evolution strategies | CMA-ES, Ha, Weng | small |
| LLM-guided program evolution | FunSearch, AlphaEvolve, ShinkaEvolve, Lehman's LLM mutation | small, growing |

If the page is ever split, GA vs GP is the wrong place to cut. More natural splits:

1. **Evolutionary Computation**, as the main topic: GA, ES/CMA-ES, multi-objective, theory.
2. **Neuroevolution and Open-Ended Search**: NEAT, novelty search, quality diversity. A separate
   research community (Stanley, Lehman, Clune) with its own books and talks.
3. **Program Evolution and Algorithm Discovery**: GP, symbolic regression, and the
   FunSearch/AlphaEvolve line. The fastest-growing group, and a natural link to the LLM page.

## Recommendation

- **Now:** keep one page. Either retitle it to something like "Evolutionary Computation
  (Genetic Algorithms and Genetic Programming)", or mark the GP-specific rows so readers who come
  for GP can find them.
- **Later:** if the neuroevolution or LLM-guided evolution groups grow past roughly 10
  resources, split those off first. GP would go with the LLM-guided group, not become its own
  page.

## Comparison

| | Keep together | Split GA / GP | Split by community (recommended if it grows) |
|---|---|---|---|
| Duplicated resources | None | High (most of the shared core) | Low (each group has its own texts) |
| Size of each page | One large page | GP page thin (~9 items) | Balanced |
| Matches how the field is taught | Yes (Eiben & Smith, Luke, GECCO) | No (GP is treated as a GA variant) | Yes (matches research communities) |
| Title matches content | Partly (ES and neuroevolution not in the title) | Partly | Yes |
| Fits the site's conventions | Yes ("X and Y" topics) | Adds a near-duplicate | Adds a real "Evolutionary" section |
| Room to grow (LLM-guided evolution) | Gets crowded | Unclear which page it belongs on | Has an obvious home |
| Effort | None | Medium | Medium to high |

## Corrections against the repo

The other session only saw the live page, so a few details are off:

- It called GA/GP "the only topic in the Evolutionary section". The landing page has **no
  Evolutionary section**. The card is the last one in the AI cluster (after GenAI, before the
  crypto cluster), tied in by reciprocal cross-links with AI, ML, Deep Learning and LLMs.
  "Adds a real Evolutionary section" in the comparison would mean a new cluster in
  `#topic-grid`.
- "~55 resources" is 57 rows: 53 external and 4 on-site cross-links.

## If the user acts on this

- **Retitle:** the slug is the filename, so a new title means either keeping the old slug (a
  mismatch) or renaming the file and updating every inbound link: the four sibling pages'
  Further Reading rows, the `index.html` card and `ARCHITECTURE.md` (the prompt log and memory
  files are history and stay as written). The live GitHub Pages URL would change too, and the
  site has no redirects.
  `uv run tests/check_site.py` catches broken internal links after a rename.
- **Mark GP rows instead:** cheaper, and it keeps the URL. The template has no marker for this
  yet, so it would be a small convention change for `CLAUDE.md` and the checker (for example a
  "GP:" prefix in the note).
- **A split** would start the "Evolutionary" cluster in `index.html` and move the relevant
  cross-links. The Program Evolution page would take the LLM cross-link.
- Still open from 2026-09-25: the DSA ↔ GA/GP edge, the UCF *Abandoning Objectives* download to
  click in a browser, the new-topic suggestions and the DSA ↔ AI edge.
